import type { MemberRecord } from "@/lib/members-store";

/** Demo generator removed — members are entered manually (Supabase later). */
export function generateDemoMembersSeed(): MemberRecord[] {
  return [];
}
